(() => {
    // const html = $('html');
    // const pages = $('#pages');
    // let pageNow = 0;
    // let floatButtonBack = $('#floatButton-back');
    // let floatButtonNext = $('#floatButton-next');
    // function pageChange(rate){
    //     if(typeof rate != typeof 0) rate = this.rate; 
    //     if(pageNow + rate >= 0 && pageNow + rate < pages.childElementCount) pageNow += rate;
    //     html.style.setProperty('--pageNow', pageNow);
    //     updateFloatButton();
    // }
    // function updateFloatButton(){
    //     if(pageNow < window.preloadVariable?.getPageMax() || pageNow < pages.childElementCount-1) floatButtonNext.setAttribute('disabled', '');
    //     else floatButtonNext.removeAttribute('disabled');
    //     if(pageNow == 0) floatButtonBack.setAttribute('disabled', '');
    //     else floatButtonBack.removeAttribute('disabled');
    // }
    // updateFloatButton();
    // floatButtonBack.addEventListener('click',  pageChange.bind({rate: -1}));
    // floatButtonNext.addEventListener('click',  pageChange.bind({rate: 1}));
})();